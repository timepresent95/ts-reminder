export const isListElement = (el: Element | undefined): el is HTMLLIElement => el && el.tagName === 'LI';
export const isInputElement = (el: Element | undefined): el is HTMLLIElement => el && el.tagName === 'INPUT';
export const isButtonElement = (el: Element | undefined): el is HTMLLIElement => el && el.tagName === 'BUTTON';
